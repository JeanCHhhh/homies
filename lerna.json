{
  "packages": ["packages/*"],
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "ignoreChanges": ["*.md"],
      "allowBranch": ["master", "dev", "next"],
      "message": "chore(release): publish %s"
    }
  }
}
